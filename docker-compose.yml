services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: trannano
      POSTGRES_PASSWORD: trannano
      POSTGRES_DB: trannano
    ports:
      - "5432:5432"
    volumes:
      - trannano_pg:/var/lib/postgresql/data

volumes:
  trannano_pg:
